 {% extends 'baseadmin.html.twig' %}

{% block title %}Commande n° {{order.reference}}{% endblock %}

{% block body %}
 
 <div class="page-content">
                <div class="container-fluid">

                    <!-- start page title -->
                    <div class="row">
                        <div class="col-12">
                            <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                                <h4 class="mb-sm-0">Invoice Details</h4>

                                <div class="page-title-right">
                                    <ol class="breadcrumb m-0">
                                        <li class="breadcrumb-item"><a href="javascript: void(0);">Invoices</a></li>
                                        <li class="breadcrumb-item active">Invoice Details</li>
                                    </ol>
                                </div>

                            </div>
                        </div>
                    </div>
                    <!-- end page title -->

                    <div class="row justify-content-center">
                        <div class="col-xxl-9">
                            <div class="card" id="demo">
                                <div class="row">
                                    <div class="col-lg-12">
                                        <div class="card-header border-bottom-dashed p-4">
                                            <div class="d-flex">
                                                <div class="flex-grow-1">
                                                    <img src="{{asset('hg-dev.png')}}" class="card-logo card-logo-dark" alt="logo dark" height="17">
                                                    <img src="{{asset('hg-dev.png')}}" class="card-logo card-logo-light" alt="logo light" height="17">
                                                    <div class="mt-sm-5 mt-4">
                                                        <h6 class="text-muted text-uppercase fw-semibold">Address</h6>
                                                        <p class="text-muted mb-1" id="address-details">{{order.address}}</p>
                                                        <p class="text-muted mb-0" id="zip-code"><span>Zip-code:</span> 90201</p>
                                                    </div>
                                                </div>
                                                <div class="flex-shrink-0 mt-sm-0 mt-3">
                                                   
                                                    <h6><span class="text-muted fw-normal">Email:</span><span id="email">{{order.user.email}}</span></h6>
                                                    <h6><span class="text-muted fw-normal">Website:</span> <a href="https://hatemghorbel.com/" class="link-primary" target="_blank" id="website">www.hatemghorbel.com</a></h6>
                                                    <h6 class="mb-0"><span class="text-muted fw-normal">Contact No: </span><span id="contact-no"> +(216) 55 234 679</span></h6>
                                                </div>
                                            </div>
                                        </div>
                                        <!--end card-header-->
                                    </div><!--end col-->
                                    <div class="col-lg-12">
                                        <div class="card-body p-4">
                                            <div class="row g-3">
                                                <div class="col-lg-3 col-6">
                                                    <p class="text-muted mb-2 text-uppercase fw-semibold">Invoice No</p>
                                                    <h5 class="fs-14 mb-0">#VL<span id="invoice-no">{{order.reference}}</span></h5>
                                                </div>
                                                <!--end col-->
                                                <div class="col-lg-3 col-6">
                                                    <p class="text-muted mb-2 text-uppercase fw-semibold">Date</p>
                                                    <h5 class="fs-14 mb-0"><span id="invoice-date">{{order.createdAt | date('d/m/Y')}}</span> <small class="text-muted" id="invoice-time">{{order.createdAt | date('H:m')}}</small></h5>
                                                </div>
                                                <!--end col-->
                                                <div class="col-lg-3 col-6">
                                                    <p class="text-muted mb-2 text-uppercase fw-semibold">Statut de paiement</p>
                                                    <span class="badge badge-soft-success fs-11" id="payment-status">{% if order.etat == 0 %} non payee {% else %} Payée {% endif %}</span>
                                                </div>
                                                <!--end col-->
                                                <div class="col-lg-3 col-6">
                                                    <p class="text-muted mb-2 text-uppercase fw-semibold">Montant total</p>
                                                    <h5 class="fs-14 mb-0"><span id="total-amount">{{order.total}} DT</span></h5>
                                                </div>
                                                <!--end col-->
                                            </div>
                                            <!--end row-->
                                        </div>
                                        <!--end card-body-->
                                    </div><!--end col-->
                                    <div class="col-lg-12">
                                        <div class="card-body p-4 border-top border-top-dashed">
                                            <div class="row g-3">
                                                <div class="col-6">
                                                    <h6 class="text-muted text-uppercase fw-semibold mb-3">Adresse de facturation</h6>
                                                    <p class="fw-medium mb-2" id="billing-name">{{order.user.nom}} {{order.user.prenom}}</p>
                                                    <p class="text-muted mb-1" id="billing-address-line-1">{{order.address}}</p>
                                                    <p class="text-muted mb-1"><span>Telephone: +</span><span id="billing-phone-no">{{order.phone}}</span></p>
                                                  
                                                </div>
                                                <!--end col-->
                                                <div class="col-6">
                                                    <h6 class="text-muted text-uppercase fw-semibold mb-3">Adresse de livraison</h6>
                                                    <p class="fw-medium mb-2" id="shipping-name">{{order.address}}</p>
                                                    <p class="text-muted mb-1" id="shipping-address-line-1">{{order.address}}</p>
                                                    <p class="text-muted mb-1"><span>Telephone: +</span><span id="shipping-phone-no">{{order.phone}}</span></p>
                                                </div>
                                                <!--end col-->
                                            </div>
                                            <!--end row-->
                                        </div>
                                        <!--end card-body-->
                                    </div><!--end col-->
                                    <div class="col-lg-12">
                                        <div class="card-body p-4">
                                            <div class="table-responsive">
                                                <table class="table table-borderless text-center table-nowrap align-middle mb-0">
                                                    <thead>
                                                        <tr class="table-active">
                                                            <th scope="col" style="width: 50px;">#</th>
                                                            <th scope="col">Détails du produit</th>
                                                            <th scope="col">Avis</th>
                                                            <th scope="col">Quantitee</th>
                                                            <th scope="col" class="text-end">Montant</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody id="products-list">

                                                    {% for key,c in order.orderDetails %}
                                                        <tr>
                                                            <th scope="row">{{key}}</th>
                                                            <td class="text-start">
                                                                <span class="fw-medium">{{c.product}}</span>
                                                               {% for p in products %}
                                                                {% if p.id == c.imageprod %}
                                                                 {% for i in p.images  | slice(0,1) %}
                                                                  <img src="/uploads/{{i.nom}}" alt="Product" width="50">
                                                              
                                                                 {% endfor %}
                                                                 {% endif %}
                                                                {% endfor %}
                                                            </td>
                                                            <td>{{c.price}} DT</td>
                                                            <td>{{c.quantity}}</td>
                                                            <td class="text-end">{{c.total}} DT</td>
                                                        </tr>
                                                      {% endfor %}
                                                    </tbody>
                                                </table><!--end table-->
                                            </div>
                                            <div class="border-top border-top-dashed mt-2">
                                                <table class="table table-borderless table-nowrap align-middle mb-0 ms-auto" style="width:250px">
                                                    <tbody>
                                                        <tr>
                                                            <td>Soustotal</td>
                                                            <td class="text-end">{{order.total}} DT</td>
                                                        </tr>
                                                      
                                                        <tr>
                                                            <td>Livrasion</td>
                                                            <td class="text-end">{{(order.prixtransport)/ 1000}} DT</td>
                                                        </tr>
                                                        <tr class="border-top border-top-dashed fs-15">
                                                            <th scope="row">Montant Total</th>
                                                            <th class="text-end">{{((order.prixtransport)/ 1000 + (order.total))}} DT</th>
                                                        </tr>
                                                    </tbody>
                                                </table>
                                                <!--end table-->
                                            </div>
                                            <div class="mt-3">
                                                <h6 class="text-muted text-uppercase fw-semibold mb-3">DÉTAILS DE PAIEMENT::</h6>
                                                <p class="text-muted mb-1">Mode de paiement: <span class="fw-medium" id="payment-method">{{order.mode}}</span></p>
                                                <p class="text-muted mb-1">Titulaire de la carte: <span class="fw-medium" id="card-holder-name">{{order.user.nom}} {{order.user.prenom}}</span></p>
                                                <p class="text-muted mb-1">numero de transaction: <span class="fw-medium" id="card-number">{{order.stripeid}}</span></p>
                                                <p class="text-muted">Montant Total: <span class="fw-medium" id=""> </span><span id="card-total-amount">{{((order.prixtransport)/ 1000 + (order.total))}} DT</span></p>
                                            </div>
                                            <div class="mt-4">
                                                <div class="alert alert-info">
                                                    <p class="mb-0"><span class="fw-semibold">NOTES:</span>
                                                        <span id="note">Tous les comptes doivent être payés dans les 7 jours suivant la réception de la facture. A régler par chèque ou carte bancaire ou paiement direct en ligne. Si le compte n'est pas payé dans les 7 jours, les détails des crédits fournis à titre de confirmation des travaux entrepris seront facturés aux frais indiqués ci-dessus.
                                                        </span>
                                                    </p>
                                                </div>
                                            </div>
                                            <div class="hstack gap-2 justify-content-end d-print-none mt-4">
                                                <a href="javascript:window.print()" class="btn btn-success"><i class="ri-printer-line align-bottom me-1"></i> Print</a>
                                                <a href="javascript:void(0);" class="btn btn-primary"><i class="ri-download-2-line align-bottom me-1"></i> Download</a>
                                            </div>
                                        </div>
                                        <!--end card-body-->
                                    </div><!--end col-->
                                </div><!--end row-->
                            </div>
                            <!--end card-->
                        </div>
                        <!--end col-->
                    </div>
                    <!--end row-->

                </div><!-- container-fluid -->
            </div><!-- End Page-content -->
            {% endblock %}